---
const domains = [
  {
    name: 'Robotics',
    description: 'Mechanical systems, sensors, automation, embedded systems, cobots, LeRobots, TurtleBots',
    icon: '🤖'
  },
  {
    name: 'Physical AI',
    description: 'Human-robot interaction, gesture-controlled systems, AI-integrated hardware',
    icon: '🧠'
  },
  {
    name: 'Drone Technology',
    description: 'Design, assemble, fly, program drones (quadcopter, FPV, etc.)',
    icon: '🚁'
  },
  {
    name: 'Space Technology',
    description: 'Satellite simulation, basic astrophysics, trajectory models, rover design',
    icon: '🚀'
  },
  {
    name: 'CAD & Prototyping',
    description: '3D modeling, 3D printing, laser cutting, rapid prototyping',
    icon: '🖨️'
  },
];
---

<section class="section-padding bg-dark-blue-light relative overflow-hidden">
  <div class="container-max">
    <div class="text-center mb-12 fade-in">
      <h2 class="text-4xl md:text-5xl font-black mb-4 text-white">
        TECHNOLOGY DOMAINS COVERED
      </h2>
      <p class="text-cyan text-lg md:text-xl max-w-2xl mx-auto">
        Comprehensive tech education across cutting-edge domains
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
      {domains.map((domain, index) => (
        <div class="fade-in" style={`transition-delay: ${index * 100}ms`}>
          <div class="bg-dark-blue border-2 border-cyan/30 rounded-lg p-6 md:p-8 h-full hover:border-cyan transition-all hover:transform hover:scale-105 text-center">
            <div class="w-20 h-20 bg-cyan/20 border-2 border-cyan/50 rounded-lg flex items-center justify-center mx-auto mb-4">
              <span class="text-4xl">{domain.icon}</span>
            </div>
            <h3 class="text-2xl font-bold text-white mb-3">
              {domain.name}
            </h3>
            <p class="text-white/80 leading-relaxed">
              {domain.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const fadeElements = document.querySelectorAll('.fade-in');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1 });

    fadeElements.forEach(el => observer.observe(el));
  });
</script>

